export enum VideoVisibility {
  PUBLIC = "public",
  PRIVATE = "private",
}

export enum QueryKey {
  VIDEOS = "videos",
  PUBLIC = "public",
  USER = "user",
}

export enum MenuAction {
  EDIT = "edit",
  DELETE = "delete",
  DOWNLOAD = "download",
  SHARE = "share",
}

export const SESSION_VIEWED_VIDEOS = "viewedVideos"